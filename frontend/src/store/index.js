import { createStore } from 'vuex'

// Pas besoin d'importer Vue ou d'utiliser Vue.use(Vuex)

export default createStore({
  state: {
    // Définissez votre état global ici
  },
  mutations: {
    // Définissez vos mutations pour modifier l'état
  },
  actions: {
    // Actions pour les opérations asynchrones
  },
  getters: {
    // Getters pour accéder à l'état
  },
})
